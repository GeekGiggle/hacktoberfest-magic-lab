DreamFrame — upload images, get a GIF.

Install:
python3 -m venv venv
. venv/bin/activate
pip install -r requirements.txt
python app.py

Contribute: add one preset or a simple CSS style change as your PR.
